version: "3.9"

services:
  myocortex:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./myocortex_cache.db:/app/myocortex_cache.db
    restart: unless-stopped
